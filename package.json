{
  "name": "simple-json-server-test",
  "version": "1.0.0",
  "description": "simple json mock server test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch db.json --host 0.0.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
